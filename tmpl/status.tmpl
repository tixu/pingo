<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Vue.JS Tutorial</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  </head>

  <body>
    <div id="app" class = "page-header">
      <h1>Pingo : smals monitoring application</small></h1>
	  </div>
     <div class="panel panel-default">
	   <div id="heading" class="panel-heading">
                    <h3 class="panel-title">Status</h3>
      <p> Nombre de systeme monitoré : <span class="badge"> {{ nbr }} </span></p>
      </div>
      <div class="panel-body">
        <div id="app-4" >
          <table class="table">   
            <tr>
              <th>Name</th>
              <th>Online</th>
              <th>Since</th>
              <th>Update</th>
            </tr>
             <tr v-for="todo in todos">
             <td>{{ todo.Target.Name }}</td> 
             <td> <p v-if="todo.Online"><span class="label label-success"> Alive </span></p> <p v-else><span class="label label-danger">Dead</span></p></td>
             <td>{{ todo.Since }}</td>
             <td>{{ todo.LastCheck }}</td>
            </tr>
          </table>
       </div>
      </div>
      
			<div id ="footer" class="panel-footer">{{ message }} </div>
      </div>
	
    
  </body>

  <script src="https://unpkg.com/vue@2.0.3/dist/vue.js"></script>
<script type="text/javascript">
  var timeout = setTimeout("location.reload(true);",10000);
  function resetTimeout() {
    clearTimeout(timeout);
    timeout = setTimeout("location.reload(true);",10000);
  }
</script>
  <script>
   var fruits = [];
    <% range $key, $value := .StateHolder.State %>
           fruits.push(<% json . | printf "%s"  %>);
      <% end %>
  
    var app = new Vue({
      el: '#heading',
      data: {
	    	nbr: <% len .StateHolder.State%>
      }
    })
var list = new Vue({
  el: '#app-4',
  data: {
    todos: fruits 
}})

var footer = new Vue({
  el: '#footer',
  data: {
        message: 'version <% .Version%> - build : hash <% .Hash%>' 
}})


  </script>
</html>